{
  "//": "Os testes vivem entre dois mundos: mexem no DOM (jsdom) e também leem arquivos do disco, como o teste que compara os tokens dos dois temas dentro do index.css. A configuração da aplicação só conhece tipos de navegador, então um `import 'node:fs'` ali quebra a compilação — foi exatamente o que derrubou o build na Vercel, onde o tsc roda limpo e não reaproveita cache incremental.",
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.test.tsbuildinfo",
    "target": "es2023",
    "lib": ["ES2023", "DOM"],
    "module": "esnext",
    "types": ["node", "vite/client"],
    "skipLibCheck": true,

    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",

    "paths": { "@/*": ["./src/*"] },

    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "erasableSyntaxOnly": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src/**/*.test.ts", "src/**/*.test.tsx", "src/tests"]
}
