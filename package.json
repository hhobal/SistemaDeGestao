{
  "name": "gestiq",
  "version": "1.0.0",
  "private": true,
  "description": "Sistema de gestão empresarial (ERP) com loja virtual integrada — painel administrativo, catálogo público e API REST.",
  "scripts": {
    "dev": "npm run dev --prefix backend",
    "dev:web": "npm run dev --prefix web",
    "start": "npm run start --prefix backend",
    "setup": "npm install --prefix backend && npm install --prefix web && npm run prisma:generate --prefix backend",
    "seed": "npm run seed --prefix backend",
    "seed:demo": "npm run seed:demo --prefix backend",
    "db:migrate": "npm run prisma:migrate --prefix backend",
    "db:studio": "npm run prisma:studio --prefix backend",
    "test": "npm test --prefix backend && npm test --prefix web"
  },
  "engines": {
    "node": ">=18"
  }
}
